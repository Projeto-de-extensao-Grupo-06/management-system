version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "8080:80"
    environment:
      - BACKEND_URL=http://api:3000
    depends_on:
      - api

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
